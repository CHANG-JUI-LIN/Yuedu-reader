.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
.build/
.build-week/
.worktrees/
build/

# Firebase CLI / Data Connect scaffolding (not used by the iOS app)
.firebase/
dataconnect/
*-debug.log
pglite-debug.log

# Local agent/skill tooling and backups
.agents/
.understand-anything/
skills-lock.json
*.bak

# Log captures from diff toolchain
scripts/logs/*.txt
scripts/logs/*.html

docs/superpowers/
.superpowers/

# OAuth / credentials
*.plist
!Info.plist
# App Store Connect API keys & signing material — NEVER commit
*.p8
AuthKey_*
*.mobileprovision
*.cer
*.certSigningRequest
serviceAccount*.json
.env
.env.*

# Xcode build artifacts
.derivedData/
*.xcworkspace/xcuserdata/

# CocoaPods (if added later)
Pods/

# Claude Code skills (local dev only)
.claude/

# Agent instructions (local dev only)
AGENTS.md

# Local SPM package forks (local dev only)
LocalPackages/
CLAUDE.md
.codex/

# Local theme-pack sample material (IP images, ~52MB) — not shipped; theme packs deferred
Resources/Assets/ReaderThemes/
